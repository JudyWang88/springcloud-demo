server:
  port: 8080
spring:
  application:
    name: springcloud.learninig
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        group: dev
      discovery:
        server-addr: localhost:8848





