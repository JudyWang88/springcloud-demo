server:
  port: 8081
spring:
  application:
    name: springcloud.client
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        group: dev
      discovery:
        server-addr: localhost:8848





